:80 {
    encode gzip zstd

    @api path /api/* /openapi.json /docs /redoc
    handle @api {
        reverse_proxy api:8000
    }

    @ws path /ws/*
    handle @ws {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
